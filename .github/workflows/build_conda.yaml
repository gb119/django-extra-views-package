
name: Conda

on:
  push

# workflow_dispatch:        # Un comment line if you also want to trigger action manually

permissions:
  contents: read

jobs:
  conda_deployment_with_new_tag:
    name: Conda deployment of package for platform ${{ matrix.os }} with Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Conda environment creation and activation
        uses: conda-incubator/setup-miniconda@v3
        with:
          python-version: "3.12"
          environment-file: env.yaml
          activate-environment: build
          auto-update-conda: true
          auto-activate-base: false
          show-channel-urls: true
      - name: Install conda build tools
        shell: bash -l {0}
        run: |
          conda run -n build conda install -y conda-build anaconda-client
      - name: Build conda package
        shell: bash -l {0}
        run: |
          out_dir=$(mktemp -d -t conda-build-XXXXXXXXXX)
          echo "OUT_DIR=$out_dir" >> "$GITHUB_ENV"
          conda run -n build conda build . \
            --no-anaconda-upload \
            --output-folder "$out_dir" \
            --python 3.12
      - name: Upload conda package
        shell: bash -l {0}
        env:
          ANACONDA_API_TOKEN: ${{ secrets.ANACONDA }}
        run: |
          for dirname in "$OUT_DIR"/*; do
            if [ -d "$dirname" ]; then
              for filename in "$dirname"/*; do
                if [[ "$filename" == *.tar.bz2 ]]; then
                  conda run -n build anaconda upload \
                    --user phygbu \
                    --force \
                    --label main \
                    "$filename"
                fi
              done
            fi
          done
